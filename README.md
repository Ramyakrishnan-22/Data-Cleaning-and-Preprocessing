# Data-Cleaning-and-Preprocessing
Cleaned and preprocessed the Medical Appointment No-Shows dataset by standardizing patient_id, correcting date inconsistencies, normalizing categorical values, and renaming columns. Ensured data integrity by removing duplicates and handling missing values, resulting in a consistent and analysis-ready dataset.
